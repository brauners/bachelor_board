Place your finale audio sample here as:

`public/audio/victory.mp3`

The app will try to play that sample when all games are finished.
If the file is missing or playback fails, it falls back to the built-in fanfare.

Optional:

- set `VITE_VICTORY_SAMPLE_URL` to use a different same-origin file path
